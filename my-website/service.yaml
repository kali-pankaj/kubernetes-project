apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: "my-webapp"
spec:
  type: NodePort
  selector:
    app: my-website
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007